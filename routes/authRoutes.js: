const express = require("express");
const router = express.Router();

const { vendorLogin } = require("../controllers/authController");

router.post("/vendor/login", vendorLogin);

module.exports = router;
